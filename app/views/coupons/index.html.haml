- content_for :page_title do
  Coupons

.row
  #banner
    .container.intro_wrapper
      .inner_content
        %h1.title
          Coupons For Members - The Startup Pack
        %h1.intro
          %p
            Startup Victoria members get $30,000+ worth of free/discounted access to a huge range of startup-friendly services. Each logo has a great deal behind it.
          - if logged_in?
            %p
              Click the logo to access the coupon code.
          - else
            %p
              You must be a
              = link_to("member", memberships_path)
              to access the coupon codes.

.container.container--coupons
  .row
    - @coupons.each_index do |i|
      - coupon = @coupons[i]
      .col-xs-12.col-sm-4.col-lg-3
        %a.offerslink{ href: coupon_path(coupon) }
          .inneroffer
            %div
              - if coupon.has_logo?
                = image_tag coupon.logo_path, border: "0"
              .showdet
                %br
                %h3
                  Coupon Details
                %br
                %br
                = coupon.teaser_description
            .compname
              = coupon.teaser_name
